{
  "buildCommand": "npm run vercel-build",
  "installCommand": "npm install && npx prisma generate",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "outputDirectory": ".next"
}
