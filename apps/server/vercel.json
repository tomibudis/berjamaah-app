{
  "buildCommand": "npx prisma generate && npm run build",
  "installCommand": "npm install --ignore-scripts",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
